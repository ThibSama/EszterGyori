<?php

declare(strict_types=1);

namespace Eszter\Contract;

/**
 * The generated contract artifacts are missing, corrupt or internally
 * inconsistent. Always fatal: without them this backend has no schema at all,
 * and guessing is exactly what ESZ-011 exists to prevent.
 */
final class ContractArtifactException extends \RuntimeException
{
}
